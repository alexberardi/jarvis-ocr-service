fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4
python-multipart==0.0.20
pillow==11.3.0
pytesseract==0.3.13
numpy==2.0.2
httpx==0.28.1
python-dotenv==1.2.1
redis==5.0.1
# Required, not optional: the OCR completion goes back to jarvis.recipes.jobs,
# which the recipes service consumes with an RQ worker. Without rq installed,
# queue_client falls through to a raw LPUSH onto a plain list of the same name
# that nothing reads -- the job never fails, it just never finishes. rq was in
# pyproject.toml but not here, and the image installs from this file.
rq>=1.15.1,<3.0.0
sqlalchemy>=2.0.23
alembic>=1.12.1
psycopg2-binary>=2.9.9
jarvis-settings-client @ git+https://github.com/alexberardi/jarvis-settings-client.git@9902feb

boto3>=1.28.0
requests>=2.31.0

# Optional providers (install as needed)
# Learned OCR providers. Enable one by uncommenting it here AND setting the
# matching OCR_ENABLE_* in .env — the flag alone does nothing, the package has to
# be in the image.
#
# For GPU (OCR_USE_GPU=true + the docker-compose.gpu.yaml overlay) you need the
# CUDA build of the runtime, not the CPU wheel:
#   rapidocr  -> onnxruntime-gpu       (rapidocr-onnxruntime pulls CPU onnxruntime)
#   easyocr   -> a CUDA torch build    (the default wheel is CPU-only)
#   paddleocr -> paddlepaddle-gpu
# With a CPU wheel installed the cuda flags are accepted and silently ignored.
# easyocr==1.7.0
# paddlepaddle==2.5.2
# paddleocr==2.7.0.3
rapidocr-onnxruntime>=1.3.0
# GPU: rapidocr-onnxruntime pulls CPU onnxruntime, which CONFLICTS with
# onnxruntime-gpu — installing both leaves whichever won on the path. To run
# on CUDA, install this instead of (not alongside) the CPU wheel:
#   pip uninstall -y onnxruntime && pip install onnxruntime-gpu
# onnxruntime-gpu>=1.17.0

# For Apple Vision (macOS only)
# pyobjc-framework-Vision==10.1

